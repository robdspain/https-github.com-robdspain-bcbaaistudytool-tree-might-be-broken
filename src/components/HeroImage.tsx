import React from 'react';

    const HeroImage = () => {
      return (
        <img
          src="/placeholder.svg"
          alt="Hero"
          className="object-cover w-full h-full rounded-lg"
        />
      );
    };

    export default HeroImage;
